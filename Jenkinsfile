#!groovy
@Library('freeagent') _

// The test suite is broken and has not been updated upstream. We rely on the
// apps test suite and PPT for this gem.
freeagentGem() {
    stage("Test") {
        echo "Skipping - tested by inclusion in main app"
    }
}
